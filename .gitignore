# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.so
!android/app/src/main/jniLibs/**/libi2pd.so
.gm/
.venv/
.venv*/
.audit-venv/
venv/
env/
# BlindBox daemon ships an env template under a directory literally named env/
!i2pchat/blindbox/daemon/env/
!i2pchat/blindbox/daemon/env/daemon.env.example

# Build / dist (PyInstaller etc.)
build/
dist/
*.egg-info/
*.egg
.eggs/

# Cache (tests, tools)
.pytest_cache/
.mypy_cache/
.ruff_cache/
.cache/

# Release archives (host on GitHub Releases only; avoid >100MB)
I2PChat-macOS-arm64*.zip
I2PChat-windows*.zip
I2PChat-*.AppImage
I2PChat.AppImage
I2PChat.AppDir/
appimagetool-*.AppImage
I2PChat-linux-*.zip

# Per-build integrity artifacts (attach to GitHub Releases; do not commit — differs per OS/artifact)
SHA256SUMS
SHA256SUMS.asc
SHA256SUMS.linux-*
SHA256SUMS.macos-*
SHA256SUMS.windows-*
SHA256SUMS.android

# Local tooling download
/actionlint
I2PChat.app/

# Optional local bundled-router blobs for portable release builds only.
vendor/i2pd/

# Secrets / keys — never commit
*.dat
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*_rsa
*_ed25519
secrets.json
credentials.json
*.keystore
config.local.*

# Local agent / workflow artifacts (never push)
.omx/
.omc/

# IDE / editor (Cursor — не пушить)
.cursor/
docs/CURSOR_*.md
.idea/
*.log
*.swp
*.swo
*~

# Android
android/.gradle/
android/build/
android/app/build/
android/app/.cxx/
android/local.properties
android/.cxx/
android/**/capture/

# Tests / coverage
.coverage
.coverage.*
htmlcov/

# Debian CI: copies of ../*.deb from dpkg-buildpackage (see docker-dpkg-buildpackage.yml)
debian-ci-out/

# dpkg-buildpackage / dh / pybuild (local tree only)
.pybuild/
debian/files
debian/debhelper-build-stamp
debian/*.substvars
debian/*.debhelper
debian/*.debhelper.log
debian/.debhelper/
debian/tmp/
debian/python3-i2pchat/
debian/i2pchat/
debian/i2pchat-tui/
